# TCG Script Project

This project contains two folders:

- inputs: Place your input files here.
- outputs: Output files will be saved here.

The main script is `main.py`. Run it to start your project.

## Usage

```bash
python main.py
```

Replace the code in `main.py` with your own logic as needed.
